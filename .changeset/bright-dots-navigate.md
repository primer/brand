---
'@primer/react-brand': minor
---

Updated `SubdomainNavBar` with a new minimal visual appearance and layout that replaces the previous default.

This is a visual and rendered-content breaking change. Please manually verify the update on your pages. The new default no longer applies the GitHub prefix to the site title.

To preserve the previous appearance, set `variant="full"`.
