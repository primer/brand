---
'@primer/react-brand': patch
---

Fixed first-line text clipping in accordion content, which is used across multiple components like `FAQ`, `PricingOptions`, and `RiverBreakoutTabs`.

Also removed the `--brand-Accordion-content-offset` CSS variable as it's no longer required.
